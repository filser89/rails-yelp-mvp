<div class="container">
  <h3><%= @restaurant.name %></h3>

  <%= simple_form_for [restaurant, review] do |f| %>
      <%= f.input :rating, collection: Review::RATING, as: "integer" %>
      <%= f.input :content%>
      <%= f.submit %>
  <% end %>

</div>
